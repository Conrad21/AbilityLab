import React, { Fragment } from "react";
import { Card } from "antd";

export const Card = ({ name, avatarSrc, children }) => (
  <Card bordered style={{ width: 300, float: "left", margin: 10 }}></Card>
);
